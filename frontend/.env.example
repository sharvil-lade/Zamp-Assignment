# Where the FastAPI backend lives. Empty means "same origin", which is what a
# production build uses. In development the two run on different ports, so the
# backend must list this origin in CORS_ORIGINS for the browser to allow the call.
VITE_API_BASE=http://localhost:8000
