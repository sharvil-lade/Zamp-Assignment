"""Everything that talks to a model.

Document extraction, the ambiguous-band name comparator, and the AI
Employee itself. Nothing in here decides a status: it produces evidence
and prose, and `engine.rules` judges both.
"""
